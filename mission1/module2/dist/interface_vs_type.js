"use strict";
{
    let user = {
        name: "Tamal",
        age: 24,
        role: "admin",
    };
    let numArray = [1, 2, 3, 4, 5];
    let numArray2 = [1, 2, 3, 4, 5];
    console.log(numArray, numArray2);
    let add = (num1, num2) => {
        return num1 + num2;
    };
}
